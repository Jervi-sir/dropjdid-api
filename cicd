shortcut for git
in macos/ubuntu

```
nano ~/.zshrc
```

```
gitpush() {
  git add .
  git commit -m "$1"
  git push origin master
  gh workflow run dropjdid
}
```

```
source ~/.zshrc
```

```
gitpush "your commit message"
```
